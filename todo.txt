# Scout V2 unattended work queue

## Current objective
Validate Stage 4 on the NUC, then continue only if it passes.

## Rules
- Work only on branch `v2-opportunity`.
- Do not modify `master`.
- Do not rerun Stage 0 or Stage 3 unless this file explicitly changes to say so.
- Do not install/upgrade packages.
- Do not use sudo.
- Do not change thresholds/config values unless explicitly instructed here.
- Keep changes minimal and stage-gated.
- If a stage FAILS: stop, write the exact failure + smallest recommended amendment into `todo_result.txt`, and do not implement the next stage.
- If a stage PASSES: write the report into `todo_result.txt` and stop unless a later task is explicitly listed below.

## Task 1 — Validate Stage 4
Repo:
`~/Documents/Projects/youtube-idea-scout`

Run:
1. `git status`
2. `git pull`
3. Confirm:
   - `stage4_opportunity.py` exists
   - Stage 4 gate settings exist in `config.py`
   - Stage 4 is wired in `scout.py`

Do NOT rerun Stage 0 or Stage 3.

Run only:
`python3 scout.py --mode opportunity --from 4`

Inspect:
- `data/opportunity_stage4_stats.json`
- `data/opportunity_gated.json`
- `data/opportunity_survivors.json`

Report:
- channels examined
- PASS count
- REVIEW count
- REJECT count
- survivor count

For each gate, report PASS / REVIEW / REJECT distribution:
- data quality
- asset dependency
- performance dependency
- cadence dependency

List every REJECT channel with:
- channel name
- candidate title
- exact gate
- reason/evidence

List every REVIEW channel with:
- channel name
- candidate title
- exact gate
- reason/evidence

Manually inspect:
- at least 5 PASS channels
- every REJECT
- every REVIEW

Check specifically:
- ambiguous dependency => REVIEW, not REJECT
- normal documentaries are not rejected merely because they may use footage
- generic words like documentary/history/news do not hard-reject by themselves
- the 3973x Nepal/spam candidate does not survive merely because of raw outlier
- FlashpointOriginal, Tech Of History, Old American Empires are not wrongly rejected
- all non-rejected records exist in `opportunity_survivors.json`
- no unexpected null/schema failures

End `todo_result.txt` with exactly one of:
`STAGE 4: PASS`
or
`STAGE 4: FAIL`

If FAIL:
- include the specific reason
- recommend the smallest amendment
- do not modify code
- do not build Stage 5

If PASS:
- do not build Stage 5 yet
- stop after writing the validation result
